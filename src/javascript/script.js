// A variable is a named container for storing data/value. You create/declare a variable using keywords var, let and const. And you can add/assign value/data (initialization) using the assignment operator (=).
// variables declared with let cannot be re-declared but can be re-assigned. Variables declared with const cannot be re-declared and cannot be re-assigned.

let myName = "Osora";
// const DOB = 1945;
const dateOfBirth = 1945;


myName = "Ekene";

console.log(myName);

// Variable naming convention
//1. variables name can container alphabets a - z and nunbers 0-9 but cannot start with a number. The only symbols allowed are _ and $.
//2. variables name cannot be a reserved keyword in js like let, var, const, if, else, for, while etc.
//3. variables name should be descriptive of the value it holds (meaningful). This is not a rule but a good practice to follow.
//4. variables name should be in camelCase. This is not a rule but a good practice to follow. Which means the fisrt word should be in lowercase and subsiquent words should start with uppercase (the very first letter should be caps)
